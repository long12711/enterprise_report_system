# 问卷选项格式更新说明

## 更新时间
2025-12-01

## 更新内容

### 1. 统一选项格式

所有题目现在使用统一的三个选项：

- ✅ **完成** - 表示该项工作已全部完成
- ⚠️ **部分完成** - 表示该项工作部分完成
- ❌ **未完成** - 表示该项工作未完成

### 2. 条件显示逻辑

**填空项显示规则**：
- 选择"完成"或"部分完成" → **显示**填空项
- 选择"未完成" → **隐藏**填空项并清空已填内容

### 3. 用户体验优化

#### 视觉反馈
- 选中的选项会高亮显示（蓝色边框）
- 填空区域平滑展开/收起动画
- 鼠标悬停时选项卡片轻微移动

#### 交互逻辑
```javascript
// 选择"完成"或"部分完成"
→ 填空区域展开
→ 用户可以填写具体数据

// 选择"未完成"  
→ 填空区域收起
→ 自动清空已填写的内容
```

### 4. 示例展示

#### 题目示例
```
序号: 1
一级指标: 治理方向性
二级指标: 党组织参与治理
三级指标: 党建融入章程

题目内容:
打分标准：完成以下内容得1分
1. 章程中明确党组织功能定位和发挥作用途径
2. 章程中明确党组织参与决策的议事规则

选项:
○ 完成
○ 部分完成  
○ 未完成

[当选择"完成"或"部分完成"时显示]
📝 请填写以下信息：
章程中党建相关条款数量（条）: [_____________]
```

### 5. 技术实现

#### 前端JavaScript
```javascript
function handleStandardOptionChange(radio) {
    const questionId = radio.dataset.questionId;
    const showBlanks = radio.dataset.showBlanks === 'true';
    
    // 控制填空区域显示
    const fillBlankArea = document.getElementById(`fillblank_${questionId}`);
    if (fillBlankArea) {
        if (showBlanks) {
            fillBlankArea.classList.add('active');  // 显示
        } else {
            fillBlankArea.classList.remove('active');  // 隐藏
            // 清空填空内容
            fillBlankArea.querySelectorAll('input').forEach(input => {
                input.value = '';
            });
        }
    }
}
```

#### HTML结构
```html
<div class="option-item">
    <input type="radio" 
           name="q_1" 
           value="完成"
           data-show-blanks="true"
           onchange="handleStandardOptionChange(this)">
    <label>完成</label>
</div>

<div class="option-item">
    <input type="radio" 
           name="q_1" 
           value="部分完成"
           data-show-blanks="true"
           onchange="handleStandardOptionChange(this)">
    <label>部分完成</label>
</div>

<div class="option-item">
    <input type="radio" 
           name="q_1" 
           value="未完成"
           data-show-blanks="false"
           onchange="handleStandardOptionChange(this)">
    <label>未完成</label>
</div>

<!-- 填空区域（默认隐藏） -->
<div class="fill-blank-area" id="fillblank_1">
    <span class="fill-blank-label">请填写以下信息：</span>
    <div class="fill-blank-item">
        <label>章程中党建相关条款数量（条）</label>
        <input type="text" name="fillblank_1_0" placeholder="请输入...">
    </div>
</div>
```

### 6. CSS样式

```css
/* 填空区域默认隐藏 */
.fill-blank-area {
    display: none;
}

/* 激活时显示并添加动画 */
.fill-blank-area.active {
    display: block;
    animation: slideDown 0.3s ease-out;
}

/* 选中的选项高亮 */
.option-item.selected {
    background: #e8eaf6;
    border: 2px solid #667eea;
}

/* 下滑动画 */
@keyframes slideDown {
    from {
        opacity: 0;
        transform: translateY(-10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
```

### 7. 访问测试

- 问卷入口: http://localhost:5000/nankai-survey
- 初级问卷: http://localhost:5000/nankai-questionnaire-fill?level=初级
- 中级问卷: http://localhost:5000/nankai-questionnaire-fill?level=中级
- 高级问卷: http://localhost:5000/nankai-questionnaire-fill?level=高级

### 8. 测试步骤

1. ✅ 打开问卷页面
2. ✅ 选择"完成" → 验证填空区域展开
3. ✅ 填写数据
4. ✅ 切换到"未完成" → 验证填空区域收起且内容清空
5. ✅ 切换到"部分完成" → 验证填空区域再次展开
6. ✅ 提交问卷 → 验证数据正确保存

### 9. 优势

1. **统一性**: 所有题目使用相同的选项格式
2. **智能性**: 根据选择自动显示/隐藏填空项
3. **友好性**: 清晰的视觉反馈和平滑动画
4. **准确性**: 未完成时自动清空填空内容，避免数据错误

## 更新完成 ✅

问卷选项已统一为"完成/部分完成/未完成"格式，填空项根据选择智能显示，用户体验得到优化！