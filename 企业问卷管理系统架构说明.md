# 企业问卷管理系统架构说明

## 📋 系统概述

本系统是为工商联用户设计的企业问卷管理平台，支持动态指标管理、问卷生成、报告模板管理和自评报告生成等功能。

---

## 🏗️ 模块架构

### 1. indicator_management/ - 指标体系管理模块
**功能定位**：管理评价体系指标，支持指标的增删改查

**核心功能**：
- ✅ 指标存储（JSON/数据库）
- ✅ 指标CRUD操作
- ✅ 三级指标体系管理
- ✅ 评分规则配置
- ✅ 适用级别设置
- ✅ 版本控制

**文件结构**：
```
indicator_management/
├── __init__.py              # 模块初始化
├── models.py                # 数据模型（已完成）
├── api.py                   # API接口（已完成）
├── services/
│   ├── __init__.py
│   ├── indicator_service.py # 指标业务逻辑
│   └── version_service.py   # 版本管理逻辑
└── data/
    ├── indicators.json      # 指标数据
    ├── scoring_rules.json   # 评分规则
    └── versions.json        # 版本信息
```

**主要接口**：
- GET/POST/PUT/DELETE `/api/admin/indicators`
- GET/POST/PUT/DELETE `/api/admin/scoring-rules`
- GET/POST `/api/admin/indicator-versions`

---

### 2. survey_generator/ - 问卷生成模块
**功能定位**：动态根据指标变化生成最新的调查问卷

**核心功能**：
- ✅ 读取最新指标体系
- ✅ 生成Excel问卷
- ✅ 生成Word问卷
- ✅ 生成HTML问卷
- ✅ 支持多级别（初级/中级/高级）
- ✅ 自动更新问卷内容

**文件结构**：
```
survey_generator/
├── __init__.py
├── generator.py                      # 通用生成器基类
├── nankai_questionnaire_generator.py # 南开问卷生成器（已完成）
├── nankai_excel_generator.py         # Excel生成器（已完成）
├── dynamic_generator.py              # 动态问卷生成器（新增）
├── template_manager.py               # 模板管理器（新增）
└── output/
    ├── excel/                        # Excel问卷输出
    ├── word/                         # Word问卷输出
    └── html/                         # HTML问卷输出
```

**工作流程**：
1. 从indicator_management读取最新指标
2. 根据指标生成问卷结构
3. 应用问卷模板
4. 输出多种格式

---

### 3. survey_engine/ - 问卷模板管理模块
**功能定位**：工商联用户管理问卷模板，发布到系统供企业填写

**核心功能**：
- ✅ 问卷模板管理
- ✅ 模板选择和发布
- ✅ 模板版本控制
- ✅ 企业问卷分配
- ✅ 填写状态跟踪

**文件结构**：
```
survey_engine/
├── __init__.py
├── api.py                    # API接口（已有）
├── models/
│   ├── __init__.py
│   ├── template.py          # 模板模型（新增）
│   └── assignment.py        # 分配模型（新增）
├── services/
│   ├── __init__.py
│   ├── template_service.py  # 模板服务（新增）
│   ├── publish_service.py   # 发布服务（新增）
│   └── loader.py            # 加载器（已有）
└── templates/
    ├── active/              # 当前活跃模板
    ├── draft/               # 草稿模板
    └── archived/            # 归档模板
```

**主要接口**：
- GET/POST/PUT/DELETE `/api/admin/survey-templates`
- POST `/api/admin/survey-templates/{id}/publish`
- POST `/api/admin/survey-templates/{id}/assign`
- GET `/api/enterprise/assigned-surveys`

---

### 4. report_generator/ - 自评报告生成模块
**功能定位**：根据企业填写的问卷生成自评报告

**核心功能**：
- ✅ 读取问卷提交数据
- ✅ 计算评分
- ✅ 生成Word报告
- ✅ 生成PDF报告
- ✅ 执行摘要
- ✅ 维度分析
- ✅ 改进建议

**文件结构**：
```
report_generator/
├── __init__.py
├── nankai_report_generator.py    # 南开报告生成器（已有）
├── report_builder.py             # 报告构建器（新增）
├── scoring_engine.py             # 评分引擎（新增）
├── analysis_engine.py            # 分析引擎（新增）
└── templates/
    ├── word_templates/           # Word模板
    └── pdf_templates/            # PDF模板
```

**工作流程**：
1. 读取问卷提交数据
2. 根据评分规则计算得分
3. 进行维度分析
4. 生成改进建议
5. 应用报告模板
6. 输出Word/PDF报告

---

### 5. report_engine/ - 报告模板管理模块
**功能定位**：动态管理报告模板，根据指标体系变化更新模板

**核心功能**：
- ✅ 报告模板管理
- ✅ 模板动态更新
- ✅ 模板版本控制
- ✅ 模板预览
- ✅ 模板导入导出

**文件结构**：
```
report_engine/
├── __init__.py
├── models/
│   ├── __init__.py
│   └── report_template.py       # 报告模板模型（新增）
├── services/
│   ├── __init__.py
│   ├── template_manager.py      # 模板管理器（新增）
│   └── dynamic_updater.py       # 动态更新器（新增）
├── templates/
│   ├── base_template.docx       # 基础模板
│   ├── executive_summary.docx   # 执行摘要模板
│   └── detailed_report.docx     # 详细报告模板
└── api.py                        # API接口（新增）
```

**主要接口**：
- GET/POST/PUT/DELETE `/api/admin/report-templates`
- POST `/api/admin/report-templates/{id}/update-from-indicators`
- GET `/api/admin/report-templates/{id}/preview`

---

## 🔄 模块间交互流程

### 流程1：指标更新 → 问卷更新
```
1. 工商联用户在indicator_management中更新指标
2. indicator_management触发更新事件
3. survey_generator监听事件，自动重新生成问卷
4. survey_engine更新问卷模板
5. 企业用户看到最新问卷
```

### 流程2：指标更新 → 报告模板更新
```
1. 工商联用户在indicator_management中更新指标
2. indicator_management触发更新事件
3. report_engine监听事件，更新报告模板
4. report_generator使用最新模板生成报告
```

### 流程3：问卷发布流程
```
1. survey_generator生成问卷
2. 工商联用户在survey_engine中选择问卷模板
3. 设置发布参数（目标企业、截止日期等）
4. 发布到系统
5. 企业用户收到通知并填写
```

### 流程4：报告生成流程
```
1. 企业用户提交问卷
2. report_generator读取提交数据
3. 从indicator_management获取评分规则
4. 计算得分和分析
5. 从report_engine获取报告模板
6. 生成最终报告
7. 发送给企业用户
```

---

## 📊 数据流向图

```
┌─────────────────────────────────────────────────────────┐
│                    工商联用户操作                          │
└─────────────────────────────────────────────────────────┘
                            │
                            ↓
┌─────────────────────────────────────────────────────────┐
│              indicator_management                        │
│              (指标体系管理)                               │
│  - 创建/编辑指标                                          │
│  - 配置评分规则                                           │
│  - 版本管理                                               │
└─────────────────────────────────────────────────────────┘
                    │                    │
                    ↓                    ↓
    ┌───────────────────────┐  ┌───────────────────────┐
    │  survey_generator     │  │  report_engine        │
    │  (问卷生成)            │  │  (报告模板管理)        │
    │  - 动态生成问卷        │  │  - 动态更新模板        │
    └───────────────────────┘  └───────────────────────┘
                    │                    │
                    ↓                    ↓
    ┌───────────────────────┐  ┌───────────────────────┐
    │  survey_engine        │  │  report_generator     │
    │  (问卷模板管理)        │  │  (报告生成)            │
    │  - 发布问卷            │  │  - 生成自评报告        │
    │  - 分配给企业          │  │  - 应用模板            │
    └───────────────────────┘  └───────────────────────┘
                    │                    │
                    ↓                    ↓
┌─────────────────────────────────────────────────────────┐
│                    企业用户操作                           │
│              填写问卷 → 提交 → 获取报告                    │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 实施计划

### 阶段1：模块重组（当前）
- [x] indicator_management 基础功能（已完成）
- [ ] 整理现有文件到对应模块
- [ ] 创建新的服务层

### 阶段2：动态生成功能
- [ ] survey_generator 动态生成器
- [ ] report_engine 动态模板更新
- [ ] 事件监听机制

### 阶段3：模板管理功能
- [ ] survey_engine 模板管理
- [ ] report_engine 模板管理
- [ ] 发布和分配功能

### 阶段4：集成测试
- [ ] 端到端流程测试
- [ ] 性能优化
- [ ] 文档完善

---

## 💡 技术要点

### 1. 动态生成机制
```python
# 监听指标变化
@indicator_updated.connect
def on_indicator_updated(indicator_id):
    # 重新生成问卷
    survey_generator.regenerate()
    # 更新报告模板
    report_engine.update_template()
```

### 2. 模板管理
```python
# 模板版本控制
class TemplateVersion:
    - version: str
    - indicator_version: str
    - template_content: dict
    - status: str (draft/active/archived)
```

### 3. 权限控制
- 工商联用户：管理指标、发布问卷、查看所有报告
- 企业用户：填写问卷、查看自己的报告

---

## 📝 下一步行动

1. **文件整理**：将现有功能文件移动到对应模块
2. **创建服务层**：为每个模块创建service层
3. **实现动态生成**：实现指标变化自动更新机制
4. **完善API**：补充缺失的API接口
5. **测试验证**：端到端测试整个流程

---

**文档版本**: v1.0.0  
**创建时间**: 2025-12-02  
**状态**: 架构设计完成，待实施